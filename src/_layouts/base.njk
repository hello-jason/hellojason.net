<!DOCTYPE html>
<html lang="en">
    <head>
        {% include "head.njk" %}
        {% if site.env %}
            <!-- capture the CSS content as a Liquid variable -->
            {% set css %}{% include "styles.min.css" %}{% endset %}
            <style>
                {{css | safe}}
            </style>
        {% else %}
            <link rel="stylesheet" href="/static/styles.min.css">
        {% endif %}

        <!-- Try and serve Google Fonts in the most efficent way possible -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;400;700&display=swap" rel="stylesheet">
    </head>
    <body class="bg-primary-400 text-black selection:bg-secondary selection:text-white page-{{ title | lower | replace(" ", "-") }}">
        <a href="#main" class="skip-link">Skip to main content</a>
        <div class="bg-hero-head bg-no-repeat {% if page.url == site.nav.home %}p-8 pb-16 md:min-h-hero-head{% else %}p-8{% endif %} md:p-5 bg-bottom">
            {% include "nav.njk" %}
            <div class="{% if page.url == site.nav.home %}pt-12 md:pt-32 text-center{% else %}pt-12 text-center md:text-left{% endif %}">
                <h1 class="{% if page.url == site.nav.home %}md:text-9xl lg:text-hero tracking-tighter{% else %}tracking-tight text-4xl lg:text-6xl{% endif %} leading-none text-white uppercase" style="text-shadow: 9px 7px 16px rgba(110 11 179 / 60%);">{{ h1 }}</h1>
                {% if subtitle %}
                    <p class="text-lg md:text-3xl lg:text-4xl md:font-thin lowercase leading-none text-center text-yellow" style="text-shadow: 3px 2px 4px rgb(110 11 179 / 80%);">{{ subtitle }}</p>
                {% endif %}
            </div>
        </div>
        <main id="main" class="bg-white p-4 pt-8 md:p-10">
            {{ content | safe }}
        </main>
        <footer class="bg-hero-feet bg-center bg-no-repeat h-48"></footer>
    </body>
    <script src="/static/app.min.js" defer></script>
</html>